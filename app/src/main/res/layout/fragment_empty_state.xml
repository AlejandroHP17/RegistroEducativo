<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_transparent"
    tools:context=".main.ui.activityMain.register.student.RegisterStudentFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:baselineAligned="false">

        <ImageView
            android:id="@+id/iv_es_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_empty_subject"
            android:contentDescription="@string/tools_image"
            android:layout_gravity="center"/>

        <TextView
            android:id="@+id/tv_es_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/empty_subject_1"
            android:layout_marginTop="@dimen/margin_extra_outer"
            android:textColor="@color/color_principal_text"
            android:textSize="@dimen/text_size_subtitle"/>

        <TextView
            android:id="@+id/tv_es_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/empty_subject_2"
            android:layout_marginTop="@dimen/margin_outer"
            android:textSize="@dimen/text_size_body"/>

        <!-- Button -->
        <include
            android:id = "@+id/include_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_extra_outer"
            android:layout_marginHorizontal="@dimen/margin_extra_outer"
            layout="@layout/include_button"/>

        </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>